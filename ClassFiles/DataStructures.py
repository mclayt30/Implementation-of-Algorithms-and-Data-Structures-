class Stack:
    # Commonly asked Stack interview questions:

    #         Evaluate postfix expression using a stack
    #         Sort values in a stack
    #         Check balanced parentheses in an expression
    
    def __init__(self):
        return         

    def Push (self): # Inserts an element at the top
        return 
    
    def Pop (self): # Returns the top element after removing from the stack
        return 
    
    def isEmpty (self): # Returns true if the stack is empty
        return 
    
    def Top (self): # Returns the top element without removing from the stack
        return 


class Queue:
    
    # Commonly asked Queue interview questions:

    #         Implement stack using a queue
    #         Reverse first k elements of a queue
    #         Generate binary numbers from 1 to n using a queue 
    
    def __init__(self): 
        return         
 
    def Enqueue(self): # Inserts an element to the end of the queue
        return
    
    def Dequeue(self): # Removes an element from the start of the queue
        return
    
    def isEmpty(self): # Returns true if the queue is empty
        return
    
    def Top(self): # Returns the first element of the help
        return


class LinkedList:
    
    # Commonly asked Linked List interview questions:

    #             Reverse a linked list
    #             Detect loop in a linked list
    #             Return Nth node from the end in a linked list
    #             Remove duplicates from a linked list
    
    def __init__(self):
        return         

    def InsertAtEnd (self): # Inserts a given element at the end of the linked list
        return
        
    def InsertAtHead (self): # Inserts a given element at the start/head of the linked list
        return
        
    def Delete (self): # Deletes a given element from the linked list
        return
        
    def DeleteAtHead (self): # Deletes the first element of the linked list
        return
        
    def Search (self): # Returns the given element from a linked list
        return
        
    def isEmpty (self): # Returns true if the linked list is empty
        return
        

class Trees:
    # Binary Tree and Binary Search Tree are the most commonly used trees.

    # Commonly asked Tree interview questions:

    #         Find the height of a binary tree
    #         Find kth maximum value in a binary search tree
    #         Find nodes at “k” distance from the root
    #         Find ancestors of a given node in a binary tree
    
    def __init__(self):
        return         


class Graphs:
    
    # In a programming language, graphs can be represented using two forms:

    #         Adjacency Matrix
    #         Adjacency List

    # Commonly asked Graph interview questions

    #         Implement Breadth and Depth First Search
    #         Check if a graph is a tree or not
    #         Count the number of edges in a graph
    #         Find the shortest path between two vertices
    
    def __init__(self):
        return         

    def DFS (self): #
        return
    
    def BFS (self): #
        return
    
    def BinarySearch (self): #
        return

class HashTables:
    
    # Commonly asked Hashing interview questions

    #         Find symmetric pairs in an array
    #         Trace complete path of a journey
    #         Find if an array is a subset of another array
    #         Check if given arrays are disjoint
    
    def __init__(self):
        return         
    
#class Tries:

# Commonly asked Trie interview questions:

#             Count total number of words in Trie
#             Print all words stored in Trie
#             Sort elements of an array using Trie
#             Form words from a dictionary using Trie
#             Build a T9 dictionary